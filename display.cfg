[mcu display]
serial: /dev/serial/by-id/usb-Klipper_stm32f042x6_02801E000C43565335383320-if00
restart_method: command

[display]
lcd_type: sh1106
i2c_mcu: display
i2c_bus: i2c1a
# Set the direction of the encoder wheel
#   Standard: Right (clockwise) scrolls down or increases values. Left (counter-clockwise scrolls up or decreases values.
encoder_pins: display:PA3, display:PA4
#   Reversed: Right (clockwise) scrolls up or decreases values. Left (counter-clockwise scrolls down or increases values.
#encoder_pins: ^display:PA4, ^display:PA3
click_pin: ^!display:PA1
kill_pin: ^!display:PA5
#x_offset: 2
#   Use X offset to shift the display towards the right. Value can be 0 to 3
#vcomh: 0
#   Set the Vcomh value on SSD1306/SH1106 displays. This value is
#   associated with a "smearing" effect on some OLED displays. The
#   value may range from 0 to 63. Default is 0.
#   Adjust this value if you get some vertical stripes on your display. (31 seems to be a good value)

[neopixel displayStatus]
pin: display:PA0
chain_count: 1
color_order: GRB
initial_RED: 0.8
initial_GREEN: 0.8
initial_BLUE: 0.8

# -----------------------------
# V0 Display Status LED Setup
# -----------------------------

[gcode_macro LED_IDLE]
gcode:
    SET_LED LED="displayStatus" RED=0.8 GREEN=0.8 BLUE=0.8


[gcode_macro LED_PRINTING]
gcode:
    SET_LED LED="displayStatus" RED=0 GREEN=1 BLUE=0

[gcode_macro LED_PAUSED]
gcode:
    SET_LED LED="displayStatus" RED=1 GREEN=1 BLUE=0

[gcode_macro LED_ERROR]
gcode:
    SET_LED LED="displayStatus" RED=1 GREEN=0 BLUE=0

[gcode_macro LED_HEATING]
gcode:
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=1
    G4 P300
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=0
    G4 P300
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=1
    G4 P300
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=0
    G4 P300
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=1
    G4 P300
    SET_LED LED="displayStatus" RED=0 GREEN=0 BLUE=0





